# FLOW//SECTOR

Next.js live market-price dashboard.

```bash
npm install
npm run dev
```

Open `http://localhost:3000`. The live Yahoo Finance proxy is at `/api/market-snapshot`.

## Preview deploy

`vercel` deploys this Next.js app as a preview. Use `vercel --prod` only for a production deployment.
